import torch

def build_sam2(config, checkpoint, device):
    # Placeholder function
    print(f"Building SAM2 model with config: {config}, checkpoint: {checkpoint}, device: {device}")
    return None

def build_sam2_video_predictor(config, checkpoint, device):
    # Placeholder function
    print(f"Building SAM2 video predictor with config: {config}, checkpoint: {checkpoint}, device: {device}")
    return None
